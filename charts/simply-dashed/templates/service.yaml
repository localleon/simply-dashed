apiVersion: v1
kind: Service
metadata:
  name: {{ include "simply-dashed.fullname" . }}
  labels:
    {{- include "simply-dashed.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simply-dashed.selectorLabels" . | nindent 4 }}
